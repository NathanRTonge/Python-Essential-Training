#from package.module import function
from numbers.factors import getFactors

print(getFactors(100))

# In __init__ the __name__ is the package name as it is the
#package file
# In factors the __name__ is package.module